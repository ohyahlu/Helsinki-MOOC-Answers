how_many = int(input("How many students on the course? "))
group_size = int(input("Desired group size? "))

formed = (how_many + group_size - 1) // group_size

print(f"Number of groups formed: {formed}")
